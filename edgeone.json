{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "agents": {
    "framework": "workflow",
    "dir": "services"
  },
  "services": {
    "framework": "workflow",
    "dir": "services"
  }
}
